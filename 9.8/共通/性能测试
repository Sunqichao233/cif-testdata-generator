性能試験 手順書（CIFファイル配置 → 集信実行）

出典：SCL_性能試験（開発機）.xlsx　No.01 / No.02
※ 本文件只写到图片里给出的 01・02 两步，03 以后等拿到后续 sheet 再补。

================================================================
这是在干什么（先看这个）
================================================================

一句话：把测试用的 CIF 文件放到 HULFT 的收件位置，跑一次「集信」，
确认文件能被正常接收并落到下游程序要读的目录。

整条链路：

  ①造数据                 ②配置                ③集信              ④落地
  run_generate.bat   →   C:\HULFT\RCV\    →   HULFT 执行      →   C:\FileRCV\output\
  （13 项 Tab 分隔）      改成指定文件名         「U2KJX001」        0000000138C*002.txt
                          U2KJX001                                        ↓
                          U2KJX002                                后续 AML/filter 取込

名词对照：

  HULFT        日本 Saison Information Systems 的文件传输中间件。
               银行系统之间传文件基本都走它，不是普通的 FTP。
  配信 / 集信   配信＝发送侧，集信＝接收侧。这次做的是「集信」（接收）。
  ファイルID    一条传输定义的编号。U2KJX001 / U2KJX002 就是。
  集信一覧      HULFT 管理画面里的集信定义列表，从这里挑一条执行。
  集信履歴      接收的执行历史。成功／失败、错误码都在这里看。
  CIFファイル   顧客情報ファイル。就是我们那个 Tab 分隔 13 项的文件。

为什么这步要单独做：性能计测之前，先确认「路能走通」。
路都不通就直接压测，测出来的数字没有意义，而且分不清是性能问题还是配置问题。

================================================================
事前準備
================================================================

0-1 准备测试用 CIF 文件

用本仓库的生成工具造：

    run_generate.bat            → output\CIF_yyyymmdd_002.txt
    （或按性能试验要求的件数）  run_generate.bat -Count 100000

※ 性能试验的件数要按试验条件来，别用默认的 100 件。
※ 文件规格（13 项 Tab 分隔 / Shift_JIS / CRLF）见仓库根目录 README.md 第 6 节。

0-2 确认资材已经反映到性能环境

前提是「② 性能環境に資材反映」已经做完（照 L&F_作業手順書_本番.xlsx 的
リリース手順_AP#1 / AP#2 / SIM 各 sheet）。没反映完就跑集信没意义。

0-3 确认连的是性能环境，不是开发环境

    横東性能AP#1   172.20.13.238
    横東性能AP#2   172.20.14.81
    横東性能SIM    172.20.13.195

开发环境是 172.20.13.247。别连错。
连接信息看 Oculus-filter AWS環境管理簿.xlsx。

⚠️ 01 和 02 全都是在这台服务器上做的。
　 手顺书里写的 C:\HULFT\RCV\ 和 C:\FileRCV\output\ 都是**服务器的** C 盘。
　 本机（自己的 PC / WorkSpaces）上没有 HULFT，在本机建这些目录没有任何作用。

================================================================
01　CIFファイル配置
================================================================

【原文】
  テスト用CIFファイルを以下のフォルダに指定のファイル名で格納する。

  C:\HULFT\RCV\
    ├ U2KJX001
    └ U2KJX002

【中文】
  把测试用的 CIF 文件，按指定的文件名放到下面这个文件夹里。

  C:\HULFT\RCV\          ← 服务器上的 C 盘，不是你本机的
    ├ U2KJX001          ← 文件名（没有扩展名）
    └ U2KJX002

【確認内容】配置できること。
【中文】　　能正常放进去就算 OK。

----------------------------------------------------------------

1-1 打开收件文件夹

    C:\HULFT\RCV\

1-2 把 0-1 造好的 CIF 文件放进去，改成指定文件名：

    U2KJX001
    U2KJX002

⚠️ 文件名没有扩展名。
　 不是 U2KJX001.txt，就是 U2KJX001。
　 Windows 默认隐藏已知扩展名，改名的时候很容易变成 U2KJX001.txt 而自己看不出来。
　 先在资源管理器打开「表示 → ファイル名拡張子」勾上，再改名。
　 改完用命令行确认一次：

    dir C:\HULFT\RCV\

1-3 确认两个文件都在、大小不是 0

⚠️ 「指定のファイル名」这句在原表里没有展开写。
　 U2KJX001 / U2KJX002 是图上直接写出来的名字，按这个放。
　 如果试验条件里另有指定（带日期、带件数之类），以那边为准。

【判定】两个文件都放进去了，没报权限错误 → OK

================================================================
02　集信実行（执行接收）
================================================================

【原文】
  HULFTを起動し、集信一覧「U2KJX001」を実施する

【中文】
  启动 HULFT，在「集信一覧」（接收定义列表）里执行「U2KJX001」这一条。
    集信　　＝ 接收（对应的发送侧叫「配信」）
    集信一覧＝ 接收定义的列表，一条定义 = 一个 ファイルID
    U2KJX001＝ 这次要跑的那条定义的编号（图上标红的就是它）

【確認内容】
  ・集信履歴にて実行した処理にエラーが発生していないこと
  ・下記のフォルダに、CIFファイルが配置されていること
    C:\FileRCV\output\0000000138C*002.txt

【中文】
  ・在「集信履歴」（接收历史）里，这次执行的处理没有报错
  ・下面这个文件夹里，CIF 文件已经落地
    C:\FileRCV\output\0000000138C*002.txt
    （* 是通配符，中间那段会变；末尾 002 对应 CIF 文件编号那一套）

----------------------------------------------------------------

2-1 启动 HULFT

⚠️ 先 RDP 连到性能环境的服务器（见 0-3）。
　 下面写的 C:\ 全都是**服务器上的** C 盘，不是你本机的。
　 本机上没装 HULFT，在本机建个 C:\HULFT\RCV\ 是白干。

HULFT 分两层，别混：

  ① 常駐デーモン（Windows 服务）… 真正干活的进程，平时一直在跑
  ② 管理画面（GUI）　　　　　　 … 人操作的界面，「集信一覧」在这里面

手顺书写的「HULFTを起動し」，实际一般是指打开 ②（管理画面）。
① 通常跟着服务器一起起来了，不用你手动启动。

a) 先确认 ① 在跑

   PowerShell：
       Get-Service | Where-Object { $_.DisplayName -like '*HULFT*' }

   或者 services.msc 里找 HULFT 开头的服务。
   状态 Running 就 OK。

   ⚠️ 如果是 Stopped，先问担当再说。别自己随手启动 —
   　 性能环境的服务状态可能是别人特意设的。

b) 打开 ② 管理画面

   本环境（横東性能AP#1 / 172.20.13.238）确认过的信息：

       版本　　　　 HULFT 8
       安装目录　　 C:\HULFT Family\hulft8\
       管理画面 exe C:\HULFT Family\hulft8\bin\hulwin.exe

   方法一：开始菜单 → 左边应用列表的「HULFT Family」文件夹 → 展开里面的条目

   方法二：直接跑 exe

       cmd：
           "C:\HULFT Family\hulft8\bin\hulwin.exe"

       PowerShell：
           & "C:\HULFT Family\hulft8\bin\hulwin.exe"

⚠️ 路径里有空格（"HULFT Family"），必须用双引号包起来。
　 不加引号会变成这样：

       C:\> C:\HULFT Family\hulft8\bin\hulwin.exe
       'C:\HULFT' は、内部コマンドまたは外部コマンド、
       操作可能なプログラムまたはバッチ ファイルとして認識されていません。

　 这是 shell 在空格处把命令断开了，把 C:\HULFT 当成了程序名 —— 不是 HULFT 坏了。
　 PowerShell 里光加引号也不行，会被当成字符串原样打印出来，必须在前面加 &。

　 或者先 cd 过去再跑，就不用管引号了：

       cd /d "C:\HULFT Family\hulft8\bin"
       hulwin.exe

2-2 打开「集信一覧」，选中 ファイルID = U2KJX001

    ※ 图上标红的就是这个 U2KJX001，别选成 U2KJX002。
      01 里两个文件都要放，但 02 只跑 U2KJX001 这一条。

2-3 执行集信

2-4 确认①：集信履歴 里这次的处理没有报错

    看这次执行的那一行，状态正常、没有错误码。
    有错误码就记下来，别继续往下走。

2-5 确认②：文件落到了输出目录

    C:\FileRCV\output\0000000138C*002.txt

    这里的 * 是通配符，中间那段会变。用命令行确认：

    dir C:\FileRCV\output\0000000138C*002.txt

    末尾的 002 对应 CIF 文件的编号（*001.txt / *002.txt / *003.txt 那套）。
    所以这次确认的是 002 这条线。

2-6 打开落地的文件粗看一眼

    Shift_JIS、CRLF、Tab 分隔 13 列，内容和 0-1 造的一致。
    编码看错会在后面的取込一起爆，这里花 10 秒能省很多事。

【判定】集信履歴无错误 ＋ 输出目录有文件 → OK

================================================================
出问题时先看哪里
================================================================

集信履歴里报错
  → 记下错误码。先确认 HULFT 服务起没起、集信定义里的路径对不对。

集信履歴没错但 output 里没文件
  → 十有八九是集信定义的输出路径和手順書写的不一致。
    对一下定义里的「受信ファイル名」。

output 里的文件是乱码
  → 编码问题。生成侧默认 Shift_JIS（config.json 的 output.encoding）。
    确认取込侧要的是不是 Shift_JIS。

文件名变成了 U2KJX001.txt
  → 01 的 1-2 那个坑。扩展名显示打开，重新改名。

================================================================
未確認事項（要问清楚）
================================================================

・常駐デーモン（服务）平时是不是一直开着。如果发现是 Stopped，
　是正常状态还是出问题了，谁来启动。

・程序装在 C:\HULFT Family\hulft8\，但手顺书里写的数据目录是 C:\HULFT\RCV\
　（两个不是同一个地方）。C:\HULFT\RCV\ 是否真的存在要确认一次：

       dir C:\HULFT\RCV\

　 如果不存在，实际的收件目录在哪里要问清楚。

・开始菜单右边那个「HULFT」磁贴点了之后一闪就消失 —— 那个磁贴指向的是什么
　（右键 → ファイルの場所を開く → プロパティ → リンク先 可以确认）。
　 走 b) 的方法二不受影响，但最好搞清楚免得下次又踩。

・「指定のファイル名」的完整规则。图上只有 U2KJX001 / U2KJX002 两个名字，
　没写是否要带日期或其他后缀。

・C:\HULFT\RCV\ 里放的文件，和集信执行之间是什么关系。
　（是从这里发出去再收回来，还是集信定义直接读这个目录）
　这点影响「集信失败时该改哪边」，最好一开始就问明白。

・0000000138C*002.txt 中间那段（* 的部分）是什么规则、由谁生成。

・U2KJX002 这条线什么时候跑。01 里两个文件都放，但 02 只写了 U2KJX001。

・性能試験的件数条件、计测项目（吞吐、处理时间等）——在 03 以后的 sheet 里，
　拿到之后补到本文件。

================================================================
接下来
================================================================

01・02 通了之后，进入 03 以后的性能計測本体。
测试用例：SCL_性能試験（開発機）.xlsx
